#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void importimage(char* imagename) {
    int resolution;
    FILE* file = NULL;
    char* currentligne[];
    
    resolution = x * y;
  
  
    file = fopen("argv[1]","r")
    if (file == NULL) {
        printf("Erreur d'ouverture du fichier \n");
    } else {
        while (i < resolution) {
          
        }
    }
